"use client";
import { useRef, useState } from "react";
import { addDays, eachMonthOfInterval, endOfMonth, format, getDay, parseISO, startOfMonth } from "date-fns";
import { Download, Plus, Printer, RotateCw, Settings2, Upload } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useApp } from "@/components/AppProvider";
import { EmptyState, Eyebrow, Metric, Panel, ProgressBar } from "@/components/ui";
import { calculateScore, currentStreak, totals } from "@/lib/metrics";
import { generateCycle, generateDailyLogs } from "@/lib/seed";
const uid=()=>crypto.randomUUID();
function Header({eyebrow,title,sub,actions}:{eyebrow:string;title:string;sub:string;actions?:React.ReactNode}){return <header className="page-title"><div><Eyebrow>{eyebrow}</Eyebrow><h1>{title}</h1><p>{sub}</p></div>{actions}</header>}
export function PlanPage(){const{data}=useApp();const cycle=data.cycles[0];return <div className="page module-page"><Header eyebrow="112 DAYS / 16 WEEKS" title="THE PLAN" sub={`${format(parseISO(cycle.startDate),"dd MMMM yyyy")} → ${format(parseISO(cycle.endDate),"dd MMMM yyyy")}`}/><div className="week-plan">{Array.from({length:16},(_,i)=>{const logs=data.dailyLogs.filter(l=>l.weekNumber===i+1);const activity=logs.filter(l=>calculateScore(l,data.habits)>0);const score=activity.length?Math.round(activity.reduce((s,l)=>s+calculateScore(l,data.habits),0)/activity.length):0;return <Panel key={i}><div className="panel-heading"><div><Eyebrow>WEEK {String(i+1).padStart(2,"0")}</Eyebrow><h3>{cycle.themes[i]}</h3></div><strong>{score}%</strong></div><ProgressBar value={score}/><small>{format(addDays(parseISO(cycle.startDate),i*7),"dd MMM")} — {format(addDays(parseISO(cycle.startDate),i*7+6),"dd MMM")}</small></Panel>})}</div></div>}
export function CalendarPage(){const{data,update}=useApp();const[selected,setSelected]=useState(data.dailyLogs[0].date);const add=(fd:FormData)=>update(d=>({...d,appointments:[...d.appointments,{id:uid(),title:String(fd.get("title")),date:selected,time:String(fd.get("time")),category:"Personal",completed:false,notes:""}]}));const months=eachMonthOfInterval({start:parseISO("2026-01-01"),end:parseISO("2026-12-31")});return <div className="page module-page"><Header eyebrow="YEAR VIEW / APPOINTMENTS" title="CALENDAR" sub="See the commitments around the mission."/><div className="calendar-layout"><Panel className="year-calendar">{months.map(month=>{const days=endOfMonth(month).getDate();return <div className="month" key={month.toISOString()}><b>{format(month,"MMM")}</b><div className="month-grid">{Array.from({length:getDay(startOfMonth(month))},(_,i)=><i key={`blank-${i}`}/>)}{Array.from({length:days},(_,i)=>{const date=format(addDays(startOfMonth(month),i),"yyyy-MM-dd");const log=data.dailyLogs.find(l=>l.date===date);const score=log?calculateScore(log,data.habits):0;return <button key={date} onClick={()=>setSelected(date)} className={`${date===selected?"selected":""} ${log?"in-cycle":""}`} style={{"--heat":`${score}%`} as React.CSSProperties} aria-label={`${date}${score?`, score ${score}%`:""}`}>{i+1}</button>})}</div></div>})}</Panel><Panel className="agenda"><Eyebrow>{format(parseISO(selected),"EEEE, dd MMMM")}</Eyebrow><h3>Agenda</h3>{data.appointments.filter(a=>a.date===selected).map(a=><button className={`appointment ${a.completed?"done":""}`} key={a.id} onClick={()=>update(d=>({...d,appointments:d.appointments.map(x=>x.id===a.id?{...x,completed:!x.completed}:x)}))}><span>{a.time||"ALL DAY"}</span><b>{a.title}</b></button>)}<form action={add}><label>Appointment<input name="title" required placeholder="Add a commitment"/></label><label>Time<input name="time" type="time"/></label><button className="button"><Plus/>Add</button></form></Panel></div></div>}
export function ProgressPage(){const{data}=useApp();const active=data.dailyLogs.filter(l=>calculateScore(l,data.habits)>0);const score=active.length?Math.round(active.reduce((s,l)=>s+calculateScore(l,data.habits),0)/active.length):0;const t=totals(data);const chart=data.dailyLogs.filter(l=>calculateScore(l,data.habits)>0).map(l=>({date:format(parseISO(l.date),"dd MMM"),score:calculateScore(l,data.habits),sleep:l.sleepHours}));return <div className="page module-page print-report"><Header eyebrow="PROGRESS / CYCLE REVIEW" title="THE PROOF" sub="Patterns you can use. Outcomes you can own." actions={<button className="button secondary no-print" onClick={()=>window.print()}><Printer/>Print report</button>}/><div className="metric-grid wide"><Metric label="Cycle adherence" value={`${score}%`} accent/><Metric label="Current streak" value={`${currentStreak(data.dailyLogs,data.habits)}d`}/><Metric label="Deep work" value={`${Math.round(data.dailyLogs.reduce((s,l)=>s+l.businessMinutes,0)/60)}h`}/><Metric label="Net cash" value={`£${t.net.toLocaleString()}`}/></div><Panel className="chart-panel"><Eyebrow>DAILY SCORE HISTORY</Eyebrow><h3>Consistency over time</h3>{chart.length?<ResponsiveContainer width="100%" height={320}><AreaChart data={chart}><defs><linearGradient id="score" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="var(--accent)" stopOpacity=".5"/><stop offset="1" stopColor="var(--accent)" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} stroke="var(--border)"/><XAxis dataKey="date"/><YAxis domain={[0,100]}/><Tooltip/><Area dataKey="score" stroke="var(--accent)" fill="url(#score)"/></AreaChart></ResponsiveContainer>:<EmptyState title="Your proof begins with one action." body="Complete something on Today and the score history will appear here."/>}</Panel><Panel><Eyebrow>RULE-BASED INSIGHTS</Eyebrow><h3>What the data can honestly say</h3><div className="insights">{active.length<7?<p>Seven active days are needed before LOCKED IN will surface behavioural observations. No invented correlations.</p>:<><p>Your strongest recorded day is {active.sort((a,b)=>calculateScore(b,data.habits)-calculateScore(a,data.habits))[0].date}.</p><p>{active.filter(l=>l.sleepHours>=7).length} recorded days include at least seven hours of sleep.</p></>}</div></Panel></div>}
export function WeeklyReviewPage(){const{data,update}=useApp();const cycle=data.cycles[0];const[week,setWeek]=useState(1);const review=data.weeklyReviews.find(r=>r.weekNumber===week);const save=(fd:FormData)=>{const next={id:review?.id??uid(),cycleId:cycle.id,weekNumber:week,wins:String(fd.get("wins")),friction:String(fd.get("friction")),lesson:String(fd.get("lesson")),nextObjective:String(fd.get("objective")),ratings:{execution:Number(fd.get("rating"))}};update(d=>({...d,weeklyReviews:[...d.weeklyReviews.filter(r=>r.weekNumber!==week),next]}));};return <div className="page module-page"><Header eyebrow="WEEKLY RESET" title="REVIEW THE SYSTEM" sub="Keep what worked. Correct what drifted. Set the next objective."/><Panel><div className="week-selector">{Array.from({length:16},(_,i)=><button className={week===i+1?"active":""} onClick={()=>setWeek(i+1)} key={i}>W{i+1}</button>)}</div><form action={save} className="review-form"><label>Wins<textarea name="wins" defaultValue={review?.wins}/></label><label>What created friction?<textarea name="friction" defaultValue={review?.friction}/></label><label>Main lesson<textarea name="lesson" defaultValue={review?.lesson}/></label><label>Next week’s business objective<input name="objective" defaultValue={review?.nextObjective}/></label><label>Execution rating<input type="range" name="rating" min="1" max="10" defaultValue={review?.ratings.execution??5}/></label><button className="button">Reset for next week</button></form></Panel></div>}
export function SettingsPage(){const{data,update,backup,importBackup}=useApp();const fileRef=useRef<HTMLInputElement>(null);const[saved,setSaved]=useState(false);const set=(key:string,value:string|number)=>{update(d=>({...d,settings:{...d.settings,[key]:value}}));setSaved(true);setTimeout(()=>setSaved(false),1200)};const startCycle=(weeks:number)=>{const latest=data.cycles[data.cycles.length-1];const cycle=generateCycle(format(addDays(parseISO(latest.endDate),1),"yyyy-MM-dd"),weeks,`LOCKED IN — CYCLE ${data.cycles.length+1}`);update(d=>({...d,cycles:[...d.cycles,cycle],dailyLogs:[...d.dailyLogs,...generateDailyLogs(cycle)]}));};return <div className="page module-page"><Header eyebrow="SYSTEM CONFIGURATION" title="SETTINGS" sub="Tune the targets. Keep the historical proof intact." actions={saved?<span className="saved">Saved</span>:undefined}/><div className="settings-grid"><Panel><div className="panel-heading"><div><Eyebrow>GENERAL</Eyebrow><h3>Profile & targets</h3></div><Settings2/></div><div className="form-grid"><label>Name<input value={data.settings.name} onChange={e=>set("name",e.target.value)}/></label><label>Water target (ml)<input type="number" value={data.settings.waterTargetMl} onChange={e=>set("waterTargetMl",Number(e.target.value))}/></label><label>Sleep target (hours)<input type="number" step=".25" value={data.settings.sleepTargetHours} onChange={e=>set("sleepTargetHours",Number(e.target.value))}/></label><label>Social limit (minutes)<input type="number" value={data.settings.socialLimitMinutes} onChange={e=>set("socialLimitMinutes",Number(e.target.value))}/></label><label>Fitness sessions / week<input type="number" value={data.settings.fitnessWeeklyTarget} onChange={e=>set("fitnessWeeklyTarget",Number(e.target.value))}/></label><label>Business hours / week<input type="number" value={data.settings.businessWeeklyHours} onChange={e=>set("businessWeeklyHours",Number(e.target.value))}/></label></div></Panel><Panel><Eyebrow>BACKUP & RESTORE</Eyebrow><h3>Your data stays yours.</h3><p>Export a complete JSON backup. Imported files are validated before replacing your local database.</p><div className="stack-actions"><button className="button" onClick={backup}><Download/>Backup my data</button><button className="button secondary" onClick={()=>fileRef.current?.click()}><Upload/>Restore backup</button><input hidden ref={fileRef} type="file" accept="application/json" onChange={e=>e.target.files?.[0]&&importBackup(e.target.files[0])}/></div><small>Last backup: {data.settings.lastBackupAt?format(parseISO(data.settings.lastBackupAt),"dd MMM yyyy, HH:mm"):"Not yet"}</small></Panel><Panel><Eyebrow>START NEXT CYCLE</Eyebrow><h3>History is never overwritten.</h3><div className="cycle-buttons">{[4,8,12,16].map(weeks=><button key={weeks} onClick={()=>startCycle(weeks)}><RotateCw/>{weeks} weeks</button>)}</div></Panel></div></div>}
